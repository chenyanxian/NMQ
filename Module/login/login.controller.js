'use strict';

angular.module('app')
    .controller('loginCtrl', function ($http,$rootScope,$scope,$stateParams,$state,core) {
        $rootScope.show = true;
        $scope.test = "loginBg"

    });

